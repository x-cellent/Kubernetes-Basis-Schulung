apiVersion: v1
kind: Service
metadata:
  name: {{ include "webserver2.fullname" . }}
  labels:
    {{- include "webserver2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webserver2.selectorLabels" . | nindent 4 }}
